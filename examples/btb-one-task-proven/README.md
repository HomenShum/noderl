# Example — one BankerToolBench task, proven through a live product path

This is the **honest hero**: not "100/100", but one task with a complete, machine-checkable trail.

## What this example will contain (populated after Debt 1 is settled)

- A redacted copy of the FR-020 proof receipt (`proof-receipt-contract.md` shape) with all four
  gates `pass`: fresh room → official fixture upload → public agent invocation → export/reopen →
  verifier handoff → visual judge.
- The trajectory (`trajectory.json`) for the run.
- The package manifest + boundary-box (citation) receipts.
- The visual-judge summary.

## What it deliberately does NOT contain

- Any "full suite passed" claim — the source registry marks the full-suite lane **blocked**.
- Client/confidential PDFs — the input fixtures link to the **official BankerToolBench dataset**
  rather than re-hosting financial documents.
- Per-task answer-key writers — the run is **generic-only**; materializer output, if shown, is
  labeled diagnostic.

## Why one proven task beats a claimed hundred

A single task with a resolvable evidence chain through the *real* product is reproducible and
auditable. A "100/100" headline that the proof registry contradicts is neither. This example is
the unit other teams can copy: adopt the gate list, run their own task, produce a comparable receipt.

> Populate this directory only after the honesty debts in
> `../../../docs/noderl/HONESTY_DEBTS_BEFORE_PUBLISH.md` are settled.
